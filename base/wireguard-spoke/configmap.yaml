apiVersion: v1
kind: ConfigMap
metadata:
  name: gatekey-vpn-config
  labels:
    app.kubernetes.io/name: gatekey
    app.kubernetes.io/component: wireguard-spoke
data:
  server-url: "http://gatekey-server:8080"
  hub-address: "CHANGE_ME"
  hub-port: "51820"
